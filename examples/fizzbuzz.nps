/lswitch {
1 dict begin
  /else true def

  exch
  {
    exec
    {exec /else false def exit}
    {pop}
    ifelse
  } forall

  else
  {exec}
  {pop}
  ifelse
end
} def

/fizzbuzz {
0 dict begin
  1 1 100 {
    /n exch def
    /a n 3 mod 0 eq def
    /b n 5 mod 0 eq def

    [
      {{(Fizzbuzz)=} a b and}
      {{(Fizz)=} a}
      {{(Buzz)=} b}
    ]
    {n =}
    lswitch
  } for
end
} def

fizzbuzz
